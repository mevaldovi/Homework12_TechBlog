<main>
    {{#each this}}
    <div class="container mt-3">
        <div class="card">
            <div class="card-body">
                {{text}}
                <div>
                    - {{user.username}}, {{format_time createdAt}}
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</main>